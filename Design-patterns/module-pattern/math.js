/**
 *
 *@description Split up your code into smaller, reusable pieces
 */

const sum = (x, y) => {
  return x + y;
};

const multiply = (x, y) => {
  return x * y;
};

const subtract = (x, y) => {
  return x - y;
};

const divide = (x, y) => {
  return x / y;
};

export { sum, divide, multiply, subtract };
